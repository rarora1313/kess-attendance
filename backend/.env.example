# SQLite (local dev) — change provider to "postgresql" in schema.prisma for production
DATABASE_URL="file:./dev.db"

# Production PostgreSQL example:
# DATABASE_URL="postgresql://user:password@host:5432/timesheets"

JWT_SECRET="replace-with-a-long-random-secret"
PORT=4000
